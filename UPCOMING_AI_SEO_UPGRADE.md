# 🚀 دليل تفعيل باقة الذكاء الاصطناعي وتقييمات العملاء (AI & Reviews Upgrade Pack)

> **ملاحظة للذكاء الاصطناعي (Instruction for Future AI Assistants):**
> عندما يطلب المطور تفعيل باقة الذكاء الاصطناعي والسيو أو باقة التقييمات، اتبع التعليمات الواردة في هذا الملف لدمج الفرع المحفوظ `feature/ai-reviews-seo`، وتأكد من تذكير المطور بالخطوات الخارجية المطلوبة في Google Search Console المذكورة في القسم (3).

---

## 1. أين يوجد كود الميزات الجاهز؟ وكيف نفعّله؟

جميع الأكواد والميزات مبرمجة ومختبرة وجاهزة بنسبة 100% داخل الفرع المستقل:
📂 **اسم الفرع (Branch):** `feature/ai-reviews-seo`

### أمر التفعيل والدمج عند موافقة العميل:
يتم تشغيل الأوامر التالية لدمج الميزات في الفرع الرئيسي ورفعها للموقع الحي:
```bash
git checkout main
git merge feature/ai-reviews-seo
git push origin main
```

---

## 2. ما الذي تحتويه هذه الباقة داخل الفرع؟

1. **إصلاح قراءة عداد الزبائن على السيرفر (CountUp SSR Bug):**
   - تم تعديل `components/ui/CountUp.tsx` ليرسل في كود الـ HTML الأولي الأرقام الحقيقية (`+1,585 زبون` و `99% رضا العملاء`) بدلاً من الصفر ليراها ChatGPT و Googlebot فوراً.
2. **قسم آراء وتجارب العملاء بالصفحة الرئيسية:**
   - مكوّن `components/home/CustomerReviewsSection.tsx` المدمج في `app/page.tsx` لعرض تقييمات المشترين الحقيقيين وشارة التقييم 4.9 / 5.
3. **صفحة كاملة مستقلة لآراء العملاء (`/reviews`):**
   - صفحة `app/reviews/page.tsx` مع توزيع النجوم وإحصائيات الرضا والتعليقات الحقيقية.
4. **ربط وتدعيم بيانات السكيما العالمية (JSON-LD):**
   - إضافة `aggregateRating` (4.9 من 5 بناءً على 41 تقييم معتمد) ونماذج المراجعات في `app/layout.tsx`.
5. **رندرة تقييمات المنتجات من السيرفر (SSR):**
   - نقل التقييمات من خادم صفحة المنتج إلى الـ HTML المصدري المباشر في `app/product/[slug]/page.tsx`.
6. **تحديث خريطة الموقع الـ Sitemap:**
   - إدراج رابط `/reviews` بملف `app/sitemap.ts` وترميز الروابط العربية بـ `encodeURI`.

---

## 3. الخطوات الإلزامية بعد الرفع (يجب على الـ AI تذكير المطور بها فور الدمج):

بمجرد عمل `git push` لفرع الميزات، يجب تذكير المطور بالخطوات التالية لتسريع التقاط الذكاء الاصطناعي وغوغل للبيانات الجديدة:

### أ) في Google Search Console:
1. الدخول إلى [Google Search Console](https://search.google.com/search-console) لموقع `https://www.kzora.co`.
2. من القائمة الجانبية: الذهاب إلى **ملفات السايت ماب (Sitemaps)** وكتابة `sitemap.xml` ثم الضغط على **إرسال (Submit)** لتلتقط العناكب صفحة التقييمات الجديدة فوراً.
3. في شريط البحث العلوي "فحص أي عنوان URL":
   - فحص رابط الصفحة الرئيسية `https://www.kzora.co` والضغط على **طلب الفهرسة (Request Indexing)**.
   - فحص رابط صفحة التقييمات `https://www.kzora.co/reviews` والضغط على **طلب الفهرسة (Request Indexing)**.

### ب) فحص سكيما النجوم (Rich Results Test):
- اختبار الرابط `https://www.kzora.co/reviews` عبر أداة [Google Rich Results Test](https://search.google.com/test/rich-results) للتأكد من التعرف على تقييم الـ 4.9 نجوم كـ Rich Snippet.

### ج) اختبار شات جي بي تي (ChatGPT Verification):
- بعد 24 إلى 48 ساعة من طلب الفهرسة، اختبار سؤال شات جي بي تي: *"بدي اشتري أحذية من موقع إلكتروني بسوريا"*، والتأكد من أنه يقرأ عدد الطلبيات الحقيقي وتقييم الـ 4.9 نجوم بدلاً من 0.
