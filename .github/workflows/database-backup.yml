name: Database Backup

on:
  schedule:
    # Run every Sunday at 00:00 UTC
    - cron: '0 0 * * 0'
  workflow_dispatch:
    # Allows manual trigger from the Actions tab

jobs:
  backup:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v5

      - name: Install PostgreSQL Client 17
        run: |
          sudo apt-get update
          sudo apt-get install -y postgresql-common
          sudo /usr/share/postgresql-common/pgdg/apt.postgresql.org.sh -y
          sudo apt-get update
          sudo apt-get install -y postgresql-client-17

      - name: Create Backup Directory
        run: mkdir -p ./backups

      - name: Run pg_dump (Version 17)
        env:
          # Separate the URL and the Password for better handling of special characters
          DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          PGPASSWORD: ${{ secrets.SUPABASE_DB_PASSWORD }}
        run: |
          filename="backups/kzora_backup_$(date +'%Y-%m-%d_%H-%M-%S').sql"
          # Force use of PostgreSQL 17 binary
          /usr/lib/postgresql/17/bin/pg_dump "$DB_URL" > "$filename"
          # Compress the backup
          gzip "$filename"
          echo "BACKUP_PATH=${filename}.gz" >> $GITHUB_ENV

      - name: Upload Backup Artifact
        uses: actions/upload-artifact@v5
        with:
          name: supabase-db-backup
          path: ${{ env.BACKUP_PATH }}
          retention-days: 30 # Keep for 30 days
